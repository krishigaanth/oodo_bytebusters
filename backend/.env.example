# Dayflow HRMS Backend Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection String
MONGODB_URI=mongodb://127.0.0.1:27017/dayflow_hrms

# JWT Configuration
JWT_SECRET=dayflow_super_secret_jwt_key_2026_hrms_production_grade
JWT_EXPIRES_IN=7d

# Allowed Frontend Client URL (CORS)
CLIENT_URL=http://localhost:3000

# File Upload Limit (bytes, default 5MB)
MAX_FILE_SIZE=5242880
